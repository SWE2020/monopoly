import Die
import Property

class Board:

    def __init__(self):
        #holds array of Property objects in their positions on the board
        tileArray = [][]
        #instatiates the die for the beginning of the game
        gameDie = Die()
